
실무에서 반복해서 실행되어야 하는 작업이 많음.
- 매 시간 리포트
- 5분마다 헬스체크

전통적 방식의 한계
리눅스에선 cron으로 이를 해결해 왔는데 서버 하나에 crontab을 걸어두는 방식에 문제가 있음.

단일 장애점
cron이 걸린 서버가 죽으면 작업이 안 돌음

서버에 종속
머신 환경, 패키지, 파일 경로 의존

확장이 안됨
여러 서버에 같은 crontab을 걸면 중복 실행

관리가 안됨
어느 서버의 crontab에 뭐가 걸려있는지 파악하기 어려움

- 특정 노드에 묶이지 않고
- 스케줄 정의가 선언적 관리
- 노드가 죽어도 다른 노드에서 실행
- 실행 이력을 추적

CronJob
CronJob은 스케줄에 맞춰 Job을 만들어내고 job은 Pod를 생성함
Job 위에 계층이 하나 더 생김
Job의 기능도 가짐. 재시도, 병렬 처리, 완료 인식

schedule은 기존 cron 표현식을 따름

cron이 못하던걸 해결
- 단일 장애점 없음 - CronJob은 컨트롤플레인이 관리, 생성된 Job은 임의 노드 배치
- 선언적 관리 - yaml로 정의, git으로 버전 관리
- 이력 추적 - 실행된 Job과 Pod가 오브젝트로 남아서 로그 확인

`startingDeadlineSeconds` 예정된 시각에 Job을 만들지 못했을 때, 얼마나 늦게까지는 그래도 실행할 것인가.
이 값을 설정하면 그 시간 안이면 늦게라도 실행 (너무 짧으면 그 회차를 건너뜀)
설정 안하면 마감이 없어서 언제든 밀린 작업을 실행하려 시도

`concurrencyPolicy` 이전 실행이 아직 안 끝났는데 다음 실행 시각이 왔을때 어떻게 할지
- Allow - 동시 실행
- Forbid - 이전 실행 안끝나면 이번 회차 건너뜀
- Replace - 이전 Job을 취소하고 새 Job으로 교체

suspend
true로 두면 새 Job 생성을 일시 중지. 이미 돌고 있는 Job엔 영향 없음. 점검 중이거나, 문제있는 CronJob을 삭제하지 않고 잠시 멈출 때

`successfulJobsHistoryLimit` `failedJobsHistoryLimit` 
완료/실패한 Job 오브젝트 몇개까지 보관할지 정함.


한번 실행하고 끝 -> Job
시간이 되면 실행 -> CronJob

주의점
- 정확한 시각 실행 보장하지 않음.
- 중복 실행 가능성
- 작업이 스케줄 간격보다 오래 걸리면
	- `concurrencyPolicy` 검토

